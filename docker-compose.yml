services:
  haus-muell-app:
    build: .
    container_name: haus-muell-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - "./data:/app/data"

